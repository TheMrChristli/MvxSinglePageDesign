<views:MvxWpfView  xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
                   x:Class="Mvx.WPF.Views.FirstView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mvx.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="LightYellow">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        
        <TextBlock Grid.ColumnSpan="2" VerticalAlignment="Center" HorizontalAlignment="Center">This is Page One!</TextBlock>
        <TextBlock Grid.ColumnSpan="2" Text="{Binding Message}" />

        <Button Content="Back" Command="{Binding CloseWindowCommand}" />

        <!--<Button Grid.Row="1" Command="{Binding ShowSecondViewCommand}" Content="ShowSecondView" />
        <Button Grid.Row="1" Grid.Column="1" Command="{Binding SetMessageCommand}" Content="ShowMessageBox" />-->
    </Grid>
</views:MvxWpfView>
